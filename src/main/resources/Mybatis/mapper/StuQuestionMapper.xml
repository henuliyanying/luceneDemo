<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mybatisdemo.mapper.StuQuestionMapper">
    <select id="getQuestionByS_num" resultType="StuQuestion" parameterType="String">
        select * from studentquestion_table where s_num=#{s_num}
    </select>
</mapper>